import { APIRequester } from '@xpla/xpla.js/dist/client/lcd/APIRequester';
import { AuthAPI, BankAPI, DistributionAPI, FeeGrantAPI, GovAPI, MarketAPI, MintAPI, AuthzAPI, OracleAPI, SlashingAPI, StakingAPI, TendermintAPI, TreasuryAPI, TxAPI, WasmAPI, IbcTransferAPI, IbcAPI } from '@xpla/xpla.js/dist/client/lcd/api';
import { LCDUtils } from '@xpla/xpla.js/dist/client/lcd/LCDUtils';
import { Wallet } from '@xpla/xpla.js/dist/client/lcd/Wallet';
import { Numeric } from '@xpla/xpla.js/dist/core/numeric';
import { Coins } from '@xpla/xpla.js/dist/core/Coins';
import { Key } from '@xpla/xpla.js/dist/key';
export interface LCDClientConfig {
    /**
     * The base URL to which LCD requests will be made.
     */
    URL: string;
    /**
     * Chain ID of the blockchain to connect to.
     */
    chainID: string;
    /**
     * Coins representing the default gas prices to use for fee estimation.
     */
    gasPrices?: Coins.Input;
    /**
     * Number presenting the default gas adjustment value to use for fee estimation.
     */
    gasAdjustment?: Numeric.Input;
    /**
     * is it connected to forked network?
     */
    isClassic?: boolean;
}
/**
 * An object repesenting a connection to a xplad node running the Lite Client Daemon (LCD)
 * server, a REST server providing access to a node.
 *
 * ### Example
 *
 * ```ts
 * import { LCDClient, Coin } from 'xpla.js';
 *
 * const xpla = new LCDClient({
 *    URL: "https://lcd.xpla.net",
 *    chainID: "dimension"
 * });
 * ```
 */
export declare class LCDClient {
    config: LCDClientConfig;
    apiRequester: APIRequester;
    auth: AuthAPI;
    bank: BankAPI;
    distribution: DistributionAPI;
    feeGrant: FeeGrantAPI;
    gov: GovAPI;
    market: MarketAPI;
    mint: MintAPI;
    authz: AuthzAPI;
    oracle: OracleAPI;
    slashing: SlashingAPI;
    staking: StakingAPI;
    tendermint: TendermintAPI;
    treasury: TreasuryAPI;
    wasm: WasmAPI;
    tx: TxAPI;
    ibc: IbcAPI;
    ibcTransfer: IbcTransferAPI;
    utils: LCDUtils;
    /**
     * Creates a new LCD client with the specified configuration.
     *
     * @param config LCD configuration
     */
    constructor(config: LCDClientConfig);
    /** Creates a new wallet with the Key. */
    wallet(key: Key): Wallet;
}
