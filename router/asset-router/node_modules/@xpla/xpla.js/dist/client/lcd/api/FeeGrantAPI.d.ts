import { AccAddress } from '@xpla/xpla.js/dist/core';
import { BaseAPI } from '@xpla/xpla.js/dist/client/lcd/api/BaseAPI';
import { Allowance } from '@xpla/xpla.js/dist/core/feegrant/allowances';
import { Pagination, PaginationOptions } from '@xpla/xpla.js/dist/client/lcd/APIRequester';
import { LCDClient } from '@xpla/xpla.js/dist/client/lcd/LCDClient';
export declare class FeeGrantAPI extends BaseAPI {
    lcd: LCDClient;
    constructor(lcd: LCDClient);
    allowances(grantee: AccAddress, params?: Partial<PaginationOptions>): Promise<{
        allowances: {
            granter: AccAddress;
            grantee: AccAddress;
            allowance: Allowance;
        }[];
        pagination: Pagination;
    }>;
    allowance(granter: AccAddress, grantee: AccAddress): Promise<Allowance>;
    allowancesByGranter(granter: AccAddress, params?: Partial<PaginationOptions>): Promise<{
        allowances: {
            granter: AccAddress;
            grantee: AccAddress;
            allowance: Allowance;
        }[];
        pagination: Pagination;
    }>;
}
