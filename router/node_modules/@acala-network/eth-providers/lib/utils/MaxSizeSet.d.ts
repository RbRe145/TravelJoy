export declare class MaxSizeSet {
    private items;
    private maxSize;
    private size;
    private head;
    private tail;
    constructor(maxSize: number);
    add(blockHash: string): void;
    has(blockHash: string): boolean;
    toString(): string[];
}
