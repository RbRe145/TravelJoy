import { BigNumber, Transaction } from 'ethers';
import { ConnectionInfo } from 'ethers/lib/utils';
import { JsonRpcProvider, Networkish, TransactionReceipt, TransactionResponse } from '@ethersproject/providers';
export declare class AcalaJsonRpcProvider extends JsonRpcProvider {
    constructor(url: ConnectionInfo | string, network?: Networkish);
    _wrapTransaction(tx: Transaction, hash?: string, startBlock?: number): TransactionResponse;
    _waitForTransaction(transactionHash: string, confirmations: number, timeout: number, replaceable: {
        data: string;
        from: string;
        nonce: number;
        to: string;
        value: BigNumber;
        startBlock: number;
    }): Promise<TransactionReceipt>;
    sendTransaction(signedTransaction: string | Promise<string>): Promise<TransactionResponse>;
}
