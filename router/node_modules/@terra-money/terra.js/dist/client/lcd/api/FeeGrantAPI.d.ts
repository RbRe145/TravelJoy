import { AccAddress } from '@terra-money/terra.js/dist/core';
import { BaseAPI } from '@terra-money/terra.js/dist/client/lcd/api/BaseAPI';
import { Allowance } from '@terra-money/terra.js/dist/core/feegrant/allowances';
import { Pagination, PaginationOptions } from '@terra-money/terra.js/dist/client/lcd/APIRequester';
import { LCDClient } from '@terra-money/terra.js/dist/client/lcd/LCDClient';
export declare class FeeGrantAPI extends BaseAPI {
    lcd: LCDClient;
    constructor(lcd: LCDClient);
    allowances(grantee: AccAddress, params?: Partial<PaginationOptions>): Promise<{
        allowances: {
            granter: AccAddress;
            grantee: AccAddress;
            allowance: Allowance;
        }[];
        pagination: Pagination;
    }>;
    allowance(granter: AccAddress, grantee: AccAddress): Promise<Allowance>;
    allowancesByGranter(granter: AccAddress, params?: Partial<PaginationOptions>): Promise<{
        allowances: {
            granter: AccAddress;
            grantee: AccAddress;
            allowance: Allowance;
        }[];
        pagination: Pagination;
    }>;
}
